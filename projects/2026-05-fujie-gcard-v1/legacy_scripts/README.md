# Legacy Scripts

These scripts are historical project entrypoints kept for traceability.

The standard entrypoint for new work is the repository-level `jm` CLI. Do not
extend these scripts unless a historical workflow explicitly needs it.
