name: feature_selection
description: Feature metadata, coarse prescreening, wide SQL generation, and refinement workflow.
stages:
  - validate_config
  - feature_metadata
  - feature_prescreen
  - build_wide_sql
  - feature_refine
